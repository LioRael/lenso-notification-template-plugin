# Changelog

All notable changes to this project are documented here.

## Unreleased

- Add descriptor-first `lenso.notification-template@1`.
- Add the linked PostgreSQL template provider with immutable versions, active
  head CAS, caller-scoped idempotency, safe rendering, locale fallback, and
  operation-bound administrative Auth verification.
